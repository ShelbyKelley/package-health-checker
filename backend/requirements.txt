# Runtime dependencies for the Lambda image (and the local uvicorn server).
# Fully pinned, including transitives, so the deployed image is reproducible.
# Dev/lint/test tooling belongs in requirements-dev.txt — keep it out of here
# so it never ships to Lambda.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.5.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
mangum==0.22.0
pydantic==2.13.5
pydantic_core==2.46.5
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
