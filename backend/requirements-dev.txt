# Lint/test tooling. Layers on top of the runtime pins so the two files can
# never drift into conflicting versions of a shared dependency.
-r requirements.txt

# httpx2 is what Starlette's TestClient now prefers; without it the test run
# emits a StarletteDeprecationWarning. Test-only — the app itself still makes
# its outbound calls with httpx.
httpcore2==2.12.0
httpx2==2.12.0
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
respx==0.23.1
ruff==0.16.5
truststore==0.10.4
